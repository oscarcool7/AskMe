<%= render "check_question", question: @question %>

<div class="container">
  <h2 class="title-lg mb">Редактирование вопроса <%= @question.id %></h2>

  <%= render "form", question: @question %>
</div>
